= substring
ifndef::env-site,env-github[]
include::_attributes.adoc[]
endif::[]



[[substring1]]
== substring&#40;text: String, from: Number, until: Number&#41;: String

Selects the substring with characters that satisfy the condition:
`from &lt;= indexOf(string) < until`

_Introduced in DataWeave 2.4.0. Supported by Mule 4.4 and later._

=== Parameters
[%header, cols="1,3"]
|===
| Name | Description
| `text` | The string.
| `from` | The lowest index to include from the array.
| `until` | The lowest index to exclude from the array.
|===

=== Example

This example returns the substring with characters in indices
1, 2, 3, and 4 from the input string.

==== Source

[source,DataWeave, linenums]
----
%dw 2.0
import * from dw::core::Strings
output application/json
var text = "hello world!"
---
substring(text, 1, 5)
----

==== Output

[source,json,linenums]
----
"ello"

----


[[substring2]]
== substring&#40;text: Null, from: Any, until: Any&#41;: Null

Helper function to make `substring` work with `null`

_Introduced in DataWeave 2.4.0. Supported by Mule 4.4 and later._

