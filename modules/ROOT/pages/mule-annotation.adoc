= @Mule Annotation
ifndef::env-site,env-github[]
include::_attributes.adoc[]
endif::[]

A parameter injection annotation that can be used on component entry points and transformer methods, this annotation can be used to execute an xref:using-expressions.adoc[expression] on the message payload with the result being passed into the method.

[source,java,linenums]
----
public class MyComponent {    public Object process(@Mule("message.payload") String payload) {        // do stuff    }}
----